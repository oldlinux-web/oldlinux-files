If you have experienced programs with Debian 0.91's `syslogd', then
try getting `sysklogd-1.1.tar.gz'.  `make install' will compile and
install the new binaries in the proper place on your system.

If you want to use `klogd', then you will need to add a line to
/etc/rc.d/rc.local:

	/sbin/klogd
