This directory contains an experimental filesystem which is based on the
minix filesystem but allows for 248 character filenames and 2TB filesytems.
It is the first public release, so there may be bugs.  Caveat Emptor!

The Image file in this directory is a compiled kernel with support for
this filesystem compiled in.
  -mkj
